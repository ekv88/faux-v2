DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_NAME=root
DATABASE_USER=
DATABASE_PASSWORD=
OPENAI_API_KEY=sk-***
OPENAI_MODEL=gpt-4.1
OPENAI_SYSTEM_PROMPT=You are a senior developer assistant. You read screenshots and provide clear, step-by-step fixes.
OPENAI_USER_PROMPT=Analyze the screenshot and return ONLY valid JSON in this exact shape: {"text":"...","code":"..."}. The "text" field must be a numbered, step-by-step solution. The "code" field must include any relevant code, commands, or config. If no code is needed, return an empty string for "code". Do not include markdown fences or any extra text outside the JSON.
SERVER_ADDR=0.0.0.0:3005
